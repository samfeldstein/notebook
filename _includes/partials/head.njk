<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

  {# - <base> must be set for relative links to work.
     - If building for production (live site), use your site url (set in `_data/`).
     - Help: https://www.11ty.dev/docs/data-js/#example-exposing-environment-variables
  #}
  {% if config.environment === "production" %}
    <base href={{config.site.url}}>
  {% else %}
    <base href="http://localhost:8080/">
  {% endif %}

  <title>{{ title }} | {{ config.site.title }}</title>
  <meta
    name="description"
    content="{{ description or config.site.description }}"
/>
  <meta name="generator" content="{{ eleventy.generator }}"/>
</head>